# MessageGuardian Multi-User Bot

Telegram Business Bot для отслеживания удалённых и изменённых сообщений с поддержкой множества пользователей.

## 🚀 Возможности

- ✅ **Мультипользовательский режим** - каждый пользователь видит только свои чаты
- ✅ **Аутентификация по паролю** - защита от несанкционированного доступа
- ✅ **Блокировка после 3 попыток** - безопасность
- ✅ **Админ-панель** - управление пользователями
- ✅ **Автоудаление из БД** - после отправки уведомления
- ✅ **Сохранение всех медиа** - фото, видео, стикеры, документы, голосовые, GIF
- ✅ **View Once медиа** - сохранение исчезающих фото/видео через ответ
- ✅ **PostgreSQL** - масштабируемая база данных

## 📋 Требования

- Python 3.10+
- PostgreSQL 12+
- Telegram Premium (для Business API)

## 🔧 Установка

### 1. Клонировать репозиторий

```bash
git clone https://github.com/denly1/Secret_message1.git
cd Secret_message1
```

### 2. Создать виртуальное окружение

```bash
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

### 3. Установить зависимости

```bash
pip install -r requirements.txt
```

### 4. Настроить PostgreSQL

```bash
sudo -u postgres psql
```

```sql
CREATE DATABASE Secret_message;
CREATE USER botuser WITH PASSWORD 'your_secure_password';
GRANT ALL PRIVILEGES ON DATABASE Secret_message TO botuser;
\q
```

### 5. Создать `.env` файл

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
BOT_PASSWORD=
ADMIN_ID=your_telegram_id

# PostgreSQL Database
DB_HOST=localhost
DB_PORT=
DB_NAME=Secret_message
DB_USER=botuser
DB_PASSWORD=your_secure_password
```

### 6. Запустить бота

```bash
python bot.py
```

## 📱 Использование

### Для пользователей:

1. Напишите боту `/start`
2. Введите пароль (по умолчанию: `12391`)
3. Подключите к Telegram Business:
   - Настройки → Telegram для бизнеса
   - Раздел "Бот" → укажите @username бота
   - Выберите "Все личные чаты"
   - Включите "Сообщения 5/5"

### Команды:

- `/start` - авторизация
- `/stats` - ваша статистика
- `/help` - справка
- `/admin` - админ-панель (только для админа)

## 🗄️ База данных

Бот автоматически создаёт все необходимые таблицы при первом запуске:

- `users` - пользователи бота
- `business_connections` - связи Business API
- `messages` - временное хранилище (автоудаление)
- `failed_logins` - неудачные попытки входа
- `banned_users` - заблокированные пользователи
- `stats` - статистика по пользователям

## 🔒 Безопасность

- Пароль для доступа к боту
- Блокировка после 3 неудачных попыток
- Изоляция данных между пользователями
- Админ получает уведомления о блокировках

## 📸 View Once медиа

Для сохранения исчезающих фото/видео:
1. Ответьте на медиа любым сообщением
2. Бот автоматически скачает и отправит вам

## 🚀 Деплой на сервер

См. `DEPLOYMENT.md` для подробной инструкции по развёртыванию на production сервере.

## 📝 Лицензия

MIT License

## 👤 Автор

[@denlyz1](https://github.com/denly1)
