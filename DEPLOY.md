# Инструкция по деплою на сервер

## 1. Подключение к серверу
```bash
ssh root@148.253.213.55
```

## 2. Обновление кода
```bash
cd /root/Secret_message1
git pull origin main
```

## 3. Обновление токена бота
```bash
nano .env
```
Измените строку:
```
TELEGRAM_BOT_TOKEN=8521842450:AAECcOV-YdaTZ4Cb82O5qhaIPjFQXIJ-aeg
```
На продакшн токен:
```
TELEGRAM_BOT_TOKEN=8578409666:AAF32MDqhOvA_656XelYURy5Ok-K3RCLG_Q
```

## 4. Обновление зависимостей
```bash
pip install --upgrade aiogram
pip install -r requirements.txt
```

## 5. Перезапуск бота
```bash
systemctl restart message_guardian
systemctl status message_guardian
```

## 6. Проверка логов
```bash
journalctl -u message_guardian -f
```

## Что изменилось в этом обновлении:

### 🆕 Система подписок через Telegram Stars
✅ **Пробный период 7 дней** - автоматически для новых пользователей
✅ **Платные подписки** - Неделя (50⭐), Месяц (100⭐), Год (550⭐)
✅ **Проверка подписки** - бот не работает если подписка истекла
✅ **История платежей** - сохранение всех транзакций

### 👮 Админ-панель
✅ **Выдача подписок** - команда `grant USER_ID DAYS`
✅ **Отзыв подписок** - команда `revoke USER_ID`
✅ **Рассылка** - отправка сообщений/фото всем пользователям
✅ **Статистика** - количество пользователей и активных подписок

### 🎨 Улучшения UI/UX
✅ **Улучшен HTML-дизайн чата** - красивые пузырьки как в Telegram
✅ **Добавлено фото в приветствие** - с inline-кнопкой на инструкцию
✅ **Обновлена команда /help** - подробная инструкция
✅ **Информация о подписке** - в команде /start

### 🐛 Исправления
✅ **Исправлено определение View Once** - проверка has_media_spoiler
✅ **Улучшена обработка View Once** - логирование и проверки

## ⚠️ ВАЖНО - Обновление БД:

После `git pull` необходимо применить SQL-скрипт:

```bash
psql -U postgres -d Secret_message -f UPDATE_DB.sql
```

Это создаст новые таблицы:
- `business_connections` - подключения к Business API
- `subscriptions` - подписки пользователей
- `payment_history` - история платежей

## Важно:
- Фото для приветствия: `photo_2025-12-29_00-18-36.jpg` должно быть на сервере
- Убедитесь что папка `saved_media` существует
- База данных PostgreSQL должна быть запущена
- После обновления БД перезапустите бота
