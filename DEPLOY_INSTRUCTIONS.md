# 🚀 Полная инструкция по деплою MessageGuardian на сервер

## 📋 Предварительные требования

На сервере должно быть установлено:
- Python 3.10+
- PostgreSQL 14+
- Git
- pip

---

## 🔧 Шаг 1: Подключение к серверу

```bash
ssh your_username@your_server_ip
```

---

## 📁 Шаг 2: Переход в папку проекта

```bash
cd /root/MessageAi
# или
cd /home/your_user/MessageAi
```

---

## 🛑 Шаг 3: Остановка текущего бота

```bash
# Найти процесс бота
ps aux | grep bot.py

# Остановить процесс (замените PID на реальный ID процесса)
kill -9 PID

# Или остановить все процессы Python
pkill -f bot.py
```

---

## 📥 Шаг 4: Обновление кода с GitHub

```bash
# Сохранить текущие изменения (если есть)
git stash

# Получить последние изменения
git pull origin main

# Если были конфликты, разрешить их
git stash pop
```

---

## 🔑 Шаг 5: Обновление .env файла

```bash
# Открыть файл для редактирования
nano .env
```

**Содержимое .env файла:**

```env
# Telegram Bot Token (ВАЖНО: обновить на новый токен!)
TELEGRAM_BOT_TOKEN=your_new_bot_token_here

# Admin ID (ваш Telegram User ID)
ADMIN_ID=825042510

# Bot Password для авторизации
BOT_PASSWORD=12391

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=Secret_message
DB_USER=postgres
DB_PASSWORD=your_db_password
```

**Сохранить:** `Ctrl+O`, `Enter`, `Ctrl+X`

---

## 🗄️ Шаг 6: Обновление базы данных (если нужно)

```bash
# Проверить, есть ли файл UPDATE_DB.sql
ls -la UPDATE_DB.sql

# Если файл есть, применить миграции
psql -U postgres -d Secret_message -f UPDATE_DB.sql

# Ввести пароль от PostgreSQL когда попросит
```

**Альтернативный способ:**

```bash
# Войти в PostgreSQL
sudo -u postgres psql

# Подключиться к базе
\c Secret_message

# Выполнить SQL команды вручную (если нужно)
# Например:
# CREATE TABLE IF NOT EXISTS referrals (...);

# Выйти
\q
```

---

## 📦 Шаг 7: Установка/обновление зависимостей

```bash
# Активировать виртуальное окружение (если используется)
source venv/bin/activate

# Установить/обновить зависимости
pip install -r requirements.txt

# Установить matplotlib (если еще не установлен)
pip install matplotlib

# Проверить установленные пакеты
pip list | grep -E "aiogram|asyncpg|matplotlib|python-dotenv"
```

**Ожидаемый вывод:**
```
aiogram                3.x.x
asyncpg                0.x.x
matplotlib             3.x.x
python-dotenv          1.x.x
```

---

## 🎨 Шаг 8: Проверка файлов проекта

```bash
# Проверить наличие всех необходимых файлов
ls -la

# Должны быть:
# - bot.py (основной файл бота)
# - .env (конфигурация)
# - schema.sql (схема БД)
# - UPDATE_DB.sql (миграции)
# - requirements.txt (зависимости)
# - saved_media/ (папка для медиа)
# - photo_2025-12-29_00-18-36.jpg (фото для /start)
```

---

## 🚀 Шаг 9: Запуск бота

### Вариант 1: Запуск в фоне с nohup

```bash
# Запустить бота в фоне
nohup python3 bot.py > bot.log 2>&1 &

# Получить PID процесса
echo $!

# Сохранить PID для будущего использования
echo $! > bot.pid
```

### Вариант 2: Запуск с screen (рекомендуется)

```bash
# Создать новую screen сессию
screen -S messagebot

# Запустить бота
python3 bot.py

# Отключиться от screen (бот продолжит работать)
# Нажать: Ctrl+A, затем D

# Вернуться к screen сессии
screen -r messagebot

# Убить screen сессию (остановить бота)
screen -X -S messagebot quit
```

### Вариант 3: Создание systemd service (самый надежный)

```bash
# Создать service файл
sudo nano /etc/systemd/system/messagebot.service
```

**Содержимое файла:**

```ini
[Unit]
Description=MessageGuardian Telegram Bot
After=network.target postgresql.service

[Service]
Type=simple
User=root
WorkingDirectory=/root/MessageAi
Environment="PATH=/usr/local/bin:/usr/bin:/bin"
ExecStart=/usr/bin/python3 /root/MessageAi/bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**Команды для управления service:**

```bash
# Перезагрузить systemd
sudo systemctl daemon-reload

# Запустить бота
sudo systemctl start messagebot

# Остановить бота
sudo systemctl stop messagebot

# Перезапустить бота
sudo systemctl restart messagebot

# Включить автозапуск при загрузке сервера
sudo systemctl enable messagebot

# Проверить статус
sudo systemctl status messagebot

# Посмотреть логи
sudo journalctl -u messagebot -f
```

---

## 📊 Шаг 10: Проверка работы бота

```bash
# Посмотреть логи (если использовали nohup)
tail -f bot.log

# Или последние 50 строк
tail -n 50 bot.log

# Проверить, что бот запущен
ps aux | grep bot.py

# Проверить использование ресурсов
top -p $(cat bot.pid)
```

**Ожидаемый вывод в логах:**

```
✅ PostgreSQL connection pool created
✅ Business connections table ready
============================================================
MessageGuardian Multi-User Bot (PostgreSQL)
============================================================
🔐 Пароль: 12391
👮 Admin ID: 825042510
🗄️  База данных: Secret_message @ localhost:5432
============================================================
Бот готов! Напишите /start для авторизации
============================================================
```

---

## 🧪 Шаг 11: Тестирование функций

1. **Отправить боту `/start`** - должен прийти приветственный экран
2. **Отправить `/admin`** - должна открыться админ-панель с кнопками
3. **Нажать "Статистика прибыли"** - должны прийти красивые графики
4. **Нажать "Статистика пользователей"** - должны прийти графики пользователей
5. **Нажать "Выгрузить CSV"** - должен прийти CSV файл
6. **Проверить управление подписками** - выдать/забрать/проверить подписку

---

## 🔍 Шаг 12: Мониторинг и отладка

### Просмотр логов в реальном времени

```bash
# Если использовали nohup
tail -f bot.log

# Если использовали systemd
sudo journalctl -u messagebot -f

# Если использовали screen
screen -r messagebot
```

### Проверка ошибок

```bash
# Найти ошибки в логах
grep -i "error" bot.log
grep -i "exception" bot.log
grep -i "❌" bot.log

# Последние 100 строк с ошибками
tail -n 100 bot.log | grep -i "error"
```

### Проверка базы данных

```bash
# Подключиться к БД
sudo -u postgres psql -d Secret_message

# Проверить количество пользователей
SELECT COUNT(*) FROM users;

# Проверить активные подписки
SELECT COUNT(*) FROM subscriptions WHERE is_active = TRUE;

# Проверить платежи
SELECT SUM(amount) FROM payment_history WHERE status = 'completed';

# Выйти
\q
```

---

## 🔄 Быстрое обновление (краткая версия)

```bash
# 1. Остановить бота
sudo systemctl stop messagebot
# или
pkill -f bot.py

# 2. Обновить код
cd /root/MessageAi
git pull origin main

# 3. Обновить .env (если нужно)
nano .env

# 4. Применить миграции (если есть)
psql -U postgres -d Secret_message -f UPDATE_DB.sql

# 5. Обновить зависимости (если нужно)
pip install -r requirements.txt

# 6. Запустить бота
sudo systemctl start messagebot
# или
nohup python3 bot.py > bot.log 2>&1 &

# 7. Проверить логи
sudo journalctl -u messagebot -f
# или
tail -f bot.log
```

---

## ⚠️ Решение проблем

### Проблема: Бот не запускается

```bash
# Проверить синтаксис Python
python3 -m py_compile bot.py

# Проверить зависимости
pip install -r requirements.txt --upgrade

# Проверить .env файл
cat .env

# Запустить бота в режиме отладки
python3 bot.py
```

### Проблема: Ошибка подключения к БД

```bash
# Проверить, что PostgreSQL запущен
sudo systemctl status postgresql

# Запустить PostgreSQL
sudo systemctl start postgresql

# Проверить подключение
psql -U postgres -d Secret_message -c "SELECT 1;"
```

### Проблема: Бот не отвечает

```bash
# Проверить, что процесс запущен
ps aux | grep bot.py

# Проверить сетевое подключение
ping api.telegram.org

# Проверить токен бота
curl "https://api.telegram.org/bot<YOUR_TOKEN>/getMe"
```

### Проблема: Графики не генерируются

```bash
# Установить matplotlib
pip install matplotlib

# Проверить установку
python3 -c "import matplotlib; print(matplotlib.__version__)"

# Проверить наличие данных в БД
psql -U postgres -d Secret_message -c "SELECT COUNT(*) FROM payment_history;"
```

---

## 📝 Полезные команды

```bash
# Перезапустить бота
sudo systemctl restart messagebot

# Посмотреть статус
sudo systemctl status messagebot

# Посмотреть использование памяти
free -h

# Посмотреть место на диске
df -h

# Очистить старые логи
> bot.log

# Создать бэкап БД
pg_dump -U postgres Secret_message > backup_$(date +%Y%m%d).sql

# Восстановить БД из бэкапа
psql -U postgres -d Secret_message < backup_20250129.sql
```

---

## 🎯 Чеклист после деплоя

- [ ] Бот запущен и работает
- [ ] Логи не показывают ошибок
- [ ] `/start` работает
- [ ] `/admin` открывает админ-панель
- [ ] Графики генерируются
- [ ] CSV экспорт работает
- [ ] Управление подписками работает
- [ ] View Once фото сохраняются
- [ ] Delete Chat отправляет HTML
- [ ] Реферальная система работает

---

## 📞 Контакты для поддержки

Если возникли проблемы:
1. Проверьте логи: `tail -f bot.log`
2. Проверьте статус: `sudo systemctl status messagebot`
3. Проверьте БД: `psql -U postgres -d Secret_message`

---

**Готово! Бот успешно задеплоен! 🎉**
