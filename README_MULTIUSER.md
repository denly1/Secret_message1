# 🤖 MessageGuardian Multi-User Business Bot

Мультипользовательский бот для отслеживания удалённых и изменённых сообщений в Telegram Business чатах.

## ✨ Возможности

- 🔐 **Аутентификация по паролю** - доступ только для авторизованных пользователей
- 👥 **Мультипользовательский режим** - каждый пользователь видит только свои чаты
- 🚫 **Защита от брутфорса** - блокировка после 3 неудачных попыток
- 👮 **Админ-панель** - просмотр заблокированных пользователей и неудачных попыток входа
- 📸 **Сохранение всех медиа:**
  - Фото (включая View Once)
  - Видео (включая исчезающие)
  - Стикеры (все форматы)
  - Документы
  - Голосовые сообщения
  - Видео-кружочки
  - GIF/Анимации
- 🗑️ **Автоудаление из БД** - сообщения удаляются после отправки уведомления
- 🗄️ **PostgreSQL** - масштабируемая база данных
- 📊 **Статистика** - для каждого пользователя отдельно

## 📋 Требования

- Python 3.9+
- PostgreSQL 12+
- Telegram Bot Token
- Telegram Business аккаунт (Premium)

## 🚀 Установка

### 1. Клонируйте репозиторий

```bash
cd MessageAi
```

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

### 3. Настройте PostgreSQL

Создайте базу данных:

```sql
CREATE DATABASE Secret_message;
```

Запустите SQL-схему:

```bash
psql -U postgres -d Secret_message -f schema.sql
```

Или выполните вручную:

```sql
-- Скопируйте содержимое schema.sql и выполните в psql
```

### 4. Настройте .env файл

Отредактируйте `.env`:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
BOT_PASSWORD=12391
ADMIN_ID=ваш_telegram_id

# PostgreSQL Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=Secret_message
DB_USER=postgres
DB_PASSWORD=1
```

### 5. Запустите бота

```bash
python bot_multiuser.py
```

## 📖 Использование

### Для пользователей:

1. **Авторизация:**
   - Напишите боту `/start`
   - Введите пароль: `12391`
   - После успешной авторизации подключите бота к Telegram Business

2. **Подключение к Telegram Business:**
   - Откройте Telegram → Настройки → Telegram для бизнеса
   - Раздел "Бот" → укажите @username_вашего_бота
   - Выберите "Все личные чаты"
   - Включите разрешение "Сообщения 5/5"

3. **Команды:**
   - `/start` - авторизация
   - `/stats` - ваша статистика
   - `/help` - справка

### Для администратора:

- `/admin` - админ-панель (только для ADMIN_ID)
  - Список заблокированных пользователей
  - История неудачных попыток входа

## 🔒 Безопасность

### Защита от брутфорса:
- После 3 неудачных попыток ввода пароля пользователь блокируется
- Администратор получает уведомление о блокировке
- Все попытки логируются в базу данных

### Изоляция данных:
- Каждый пользователь видит только свои чаты
- Сообщения хранятся с привязкой к owner_id
- Автоматическое удаление из БД после отправки уведомления

## 💾 База данных

### Таблицы:

- `users` - зарегистрированные пользователи
- `messages` - временное хранилище сообщений
- `failed_logins` - история неудачных попыток
- `banned_users` - заблокированные пользователи
- `stats` - статистика по каждому пользователю

### Автоочистка:

Сообщения **автоматически удаляются** из БД после отправки уведомления владельцу. Это экономит место и повышает производительность.

## 📸 View Once фото/видео

Для сохранения исчезающих фото и видео:

1. Когда кто-то отправляет View Once медиа
2. **Ответьте на него** любым сообщением
3. Бот **сразу** скачает и отправит вам медиа
4. Не нужно ждать удаления!

## 🎥 Поддержка видео

Бот сохраняет **все типы видео:**
- ✅ Обычные видео
- ✅ Истекающие видео (с TTL)
- ✅ View Once видео (через ответ)
- ✅ Видео-кружочки
- ✅ GIF/Анимации

## 📊 Масштабируемость

### PostgreSQL vs SQLite:

| Параметр | SQLite (старая версия) | PostgreSQL (новая) |
|----------|------------------------|-------------------|
| Пользователей | 1 | Неограниченно |
| Одновременных подключений | 1 | До 20 (настраивается) |
| Размер БД | До 281 TB | Практически неограничен |
| Производительность | Хорошая для 1 пользователя | Отличная для многих |
| Автоочистка | Нет | Да |

### Рекомендации:

- **1-10 пользователей** - отлично работает
- **10-100 пользователей** - без проблем
- **100+ пользователей** - рекомендуется увеличить pool size в коде

## 🛠️ Настройка

### Изменить пароль:

В `.env`:
```env
BOT_PASSWORD=ваш_новый_пароль
```

### Изменить лимит попыток входа:

В `bot_multiuser.py` найдите:
```python
if attempts >= 3:  # Измените на нужное число
```

### Изменить размер connection pool:

В `bot_multiuser.py`:
```python
db_pool = await asyncpg.create_pool(
    ...
    min_size=5,   # Минимум подключений
    max_size=20   # Максимум подключений
)
```

## 🐛 Решение проблем

### Ошибка подключения к PostgreSQL:

```bash
# Проверьте, что PostgreSQL запущен
sudo systemctl status postgresql

# Проверьте доступ
psql -U postgres -d Secret_message
```

### Бот не сохраняет медиа:

1. Проверьте, что папка `saved_media/` существует
2. Проверьте права доступа
3. Проверьте логи бота

### Пользователь не может войти:

1. Проверьте, не заблокирован ли он: `/admin`
2. Проверьте правильность пароля в `.env`
3. Проверьте логи неудачных попыток

## 📝 Логи

Бот выводит подробные логи в консоль:

```
✅ Пользователь John (@john_doe, ID: 123456) авторизован
🔍 View Once фото обнаружено (ID: 789)
✅ View Once фото сохранено: saved_media/...
📦 Данные сообщения 789: owner=123456, media=photo_reply
✅ Фото отправлено владельцу 123456
🗑️ Сообщение 789 удалено из БД после отправки уведомления
```

## 🔄 Миграция со старой версии

Если вы использовали старую версию с SQLite:

1. Экспортируйте данные из SQLite (если нужно)
2. Создайте PostgreSQL базу по инструкции выше
3. Запустите новую версию `bot_multiuser.py`
4. Старый `bot.py` можно оставить как резервную копию

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи бота
2. Проверьте подключение к PostgreSQL
3. Проверьте `.env` файл
4. Проверьте, что бот подключён к Telegram Business

## 🎉 Готово!

Ваш мультипользовательский бот готов к работе! Теперь любое количество пользователей может использовать бота одновременно, каждый со своими чатами и статистикой.
